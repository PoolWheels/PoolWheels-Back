package com.PoolWheels;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * It's a Spring Boot application
 */
@SpringBootApplication
public class PoolWheelsApplication {

	/**
	 * The main function of the application. It is the entry point of the application.
	 */
	public static void main(String[] args) {
		SpringApplication.run(PoolWheelsApplication.class, args);
	}

}
